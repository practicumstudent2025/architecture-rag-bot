# Как настроить YandexGPT для бота

## Что нужно получить

1. **Folder ID** — идентификатор каталога в Yandex Cloud
2. **API ключ** — ключ для доступа к YandexGPT
3. **Telegram токен** — токен бота (если используете Telegram)

---

## Шаг 1: Вход в Yandex Cloud

1. Откройте https://cloud.yandex.ru/
2. Войдите через Яндекс аккаунт или зарегистрируйтесь

---

## Шаг 2: Создание каталога и получение Folder ID

1. В левом меню нажмите **"Каталоги"** (или "All resources" → "Folders")
2. Нажмите **"Создать каталог"**
3. Введите имя: `rag-bot`
4. Нажмите **"Создать"**
5. Найдите **ID каталога**:
   - В столбце "ID" в списке каталогов
   - В URL страницы (после `/folders/`)
   - В информации о каталоге (каталог → "Информация")

**Пример Folder ID:** `b1g0hfoia7megabgsds7`

**Важно:** Сохраните этот ID — он понадобится для файла `.env`

---

## Шаг 3: Создание сервисного аккаунта

1. В левом меню нажмите **"Сервисные аккаунты"** (или "IAM" → "Service accounts")
2. Нажмите **"Создать сервисный аккаунт"**
3. Введите имя: `rag-bot-service`
4. Нажмите **"Создать"**

---

## Шаг 4: Назначение роли

1. Откройте созданный сервисный аккаунт
2. Перейдите на вкладку **"Роли"**
3. Нажмите **"Назначить роли"**
4. В поле **"Роль"** выберите: `ai.languageModels.user`
5. В поле **"Каталог"** выберите ваш каталог (rag-bot)
6. Нажмите **"Сохранить"**

---

## Шаг 5: Создание API ключа

1. В сервисном аккаунте перейдите на вкладку **"Ключи"**
2. Нажмите **"Создать новый ключ"**

### Выбор типа ключа

Появится меню с тремя вариантами:
- **"Create static access key"** — для AWS-совместимого API (не подходит)
- **"Create API key"** — **выберите этот вариант** ✓
- **"Create authorized key"** — для обмена на IAM токен (не подходит)

**Выберите: "Create API key"**

### Выбор Scope (права доступа)

3. В поле **"Scope"** (или "Права доступа") нажмите на выпадающий список
4. В списке найдите и выберите: **`yc.ai.languageModels.execute`**
   - Это даст доступ к языковым моделям (YandexGPT)
   - Можете использовать поиск в списке, введя `languageModels`

**Важно:** Выберите именно `yc.ai.languageModels.execute` — это правильный scope для YandexGPT

5. Заполните описание (опционально, например: "RAG bot key")
6. Нажмите **"Создать"**
7. **Скопируйте API ключ** — он показывается только один раз

**Пример API ключа:** `AQVNxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

**Важно:** Сохраните этот ключ — он понадобится для файла `.env`

---

## Шаг 6: Получение токена Telegram-бота (опционально)

Нужно только для запуска бота в Telegram.

1. Откройте Telegram
2. Найдите бота **@BotFather**
3. Отправьте: `/start` или `/newbot`
4. Укажите имя бота (например: `Мой RAG Бот`)
5. Укажите username бота (должен заканчиваться на `bot`, например: `my_rag_bot`)
6. Скопируйте токен, который даст BotFather

**Пример токена:** `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-1234567890`

---

## Шаг 7: Создание файла .env

1. В корне проекта создайте файл **`.env`** (с точкой в начале)
   - Корень проекта — это папка, где находится файл `research_task1.md`
   - В VS Code: правый клик в корне проекта → "New File" → введите `.env`
   - В PyCharm: правый клик в корне проекта → "New" → "File" → введите `.env`

2. Откройте файл `.env` и добавьте:

```bash
LLM_PROVIDER=yandex
YANDEX_API_KEY=
YANDEX_FOLDER_ID=
TELEGRAM_BOT_TOKEN=
```

3. Замените значения на ваши:
   - После `YANDEX_API_KEY=` вставьте ваш API ключ (из шага 5)
   - После `YANDEX_FOLDER_ID=` вставьте ваш Folder ID (из шага 2)
   - После `TELEGRAM_BOT_TOKEN=` вставьте ваш Telegram токен (из шага 6)
   - Если Telegram не используете, оставьте `TELEGRAM_BOT_TOKEN=` пустым

**Пример готового файла:**
```bash
LLM_PROVIDER=yandex
YANDEX_API_KEY=AQVN1a2b3c4d5e6f7g8h9i0j1k2l3m4n5o6p7q8r9s0t
YANDEX_FOLDER_ID=b1g0hfoia7megabgsds7
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-1234567890
```

**Важно:**
- Не добавляйте пробелы вокруг знака `=`
- Не используйте кавычки вокруг значений
- Файл `.env` уже в `.gitignore`, поэтому он не попадет в git (это нормально)

---

## Запуск бота

### Консольный режим

```bash
python scripts/task4/run_rag_bot.py
```

Ожидаемый вывод:
```
Загрузка векторного хранилища...
Инициализация LLM провайдера...
RAG-бот готов к работе!
```

### Telegram-бот

```bash
python scripts/task4/run_telegram_bot.py
```

Ожидаемый вывод:
```
Загрузка векторного хранилища...
Инициализация LLM провайдера...
RAG-бот готов к работе!
Telegram-бот запущен!
```

---

## Решение проблем

### Ошибка: "YANDEX_API_KEY не установлен"
- Проверьте, что файл `.env` в корне проекта (не в подпапке)
- Проверьте, что в `.env` есть `YANDEX_API_KEY=ваш_ключ` (без пробелов)
- Убедитесь, что ключ скопирован полностью (без переносов строк)

### Ошибка: "YANDEX_FOLDER_ID не установлен"
- Проверьте, что в `.env` есть `YANDEX_FOLDER_ID=ваш_id` (без пробелов)
- Убедитесь, что Folder ID скопирован правильно

### Ошибка доступа при использовании API
- Убедитесь, что роль `ai.languageModels.user` назначена сервисному аккаунту (шаг 4)
- Проверьте, что роль назначена для правильного каталога
- Убедитесь, что выбран правильный scope `yc.ai.languageModels.execute` (шаг 5)

### Ошибка: "Invalid API key"
- Проверьте, что API ключ скопирован полностью
- Убедитесь, что нет лишних пробелов в файле `.env`
- Попробуйте создать новый API ключ

### Где найти Folder ID, если забыли
1. Откройте каталог в Yandex Cloud
2. Folder ID в URL: `https://console.cloud.yandex.ru/folders/ВОТ_ЭТОТ_ID`
3. Или: каталог → "Информация" → ID

---

## Частые вопросы

**Q: Какой тип ключа выбрать при создании?**  
A: Выберите **"Create API key"** (не static access key и не authorized key)

**Q: Какой scope выбрать?**  
A: Выберите **`yc.ai.languageModels.execute`** — это даст доступ к YandexGPT

**Q: Куда сохранить Folder ID и API ключ?**  
A: В файл `.env` в корне проекта (после знака `=` в соответствующих строках)

**Q: Файл .env не видно в редакторе**  
A: Файлы, начинающиеся с точки, могут быть скрыты. В VS Code: нажмите на иконку настроек → "Show Hidden Files". Или создайте файл через терминал: `touch .env`
